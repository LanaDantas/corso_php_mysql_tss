<<?php

    use crud\UserCRUD;
    use models\User;

    include "config.php";
    include "form_in_php/test/test_autoload.php";

    (new PDO(DB_DSN, DB_USER, DB_PASSWORD))->query("TRUNCATE TABLE user;");
    $crud = new UserCRUD();
    $user = new User();

    $user->first_name = "Mario";
    $user->last_name = "Rossi";
    $user->birthday = "2017-01-01";
    $user->birth_city = "Torino";
    $user->id_regione = "9";
    $user->id_provincia = "15";
    $user->gender = "M";
    $user->username = "mariorossi@email.com";
    $user->password = md5('Password');

    $crud->create($user);
    $user = $crud->read(1);


    $user->first_name = 'Luigi';
    $user->last_name = 'Verdi';
    $user->birthday = "12/07/1981";
    $user->birth_city = "Milano";
    $user->id_regione = "15";
    $user->id_provincia = "89";
    $user->gender = "M";
    $user->username = "luigi.vergi@email.com";
    $user->password = md5('Password');

    $crud->update($user);
    print_r($crud->read(1));